document.addEventListener('DOMContentLoaded',()=>{let p=JW.products.find(x=>x.id===new URLSearchParams(location.search).get('id'))||JW.products[0];document.title=`${p.name} | Joinware`;document.querySelector('#product').innerHTML=`<div class="product-hero-visual">${p.visual}</div><div class="product-info"><div class="breadcrumbs"><a href="./shop.html">Shop</a> / ${p.category}</div><span class="eyebrow">${p.badge}</span><h1>${p.name}</h1><p>${p.description}</p><div class="price">${JW.money(p.price)} <small>IVA esclusa</small></div><button class="btn orange" data-add="${p.id}">Aggiungi al carrello</button><div class="specs"><div><span>Sistema richiesto</span>${p.requirements}</div><div><span>Lingua</span>${p.language}</div><div><span>Attivazione</span>${p.activation}</div><div><span>Consegna</span>${p.delivery}</div></div><div class="notice"><strong>Consegna digitale manuale</strong><br>La product key non viene generata automaticamente: dopo la verifica del pagamento, Joinware la invia all’indirizzo e-mail indicato nel checkout.</div><p class="legal-note">Prima dell’acquisto verifica requisiti, edizione e compatibilità. Per licenziamenti server o configurazioni complesse, contatta l’assistenza.</p></div>`;document.querySelector('[data-add]').onclick=()=>JW.add(p.id)});
