document.addEventListener('DOMContentLoaded',()=>{let active=new URLSearchParams(location.search).get('cat')||'Tutti';const cats=['Tutti',...new Set(JW.products.map(p=>p.category))];const filters=document.querySelector('#filters'),grid=document.querySelector('#products'),search=document.querySelector('#search');filters.innerHTML=cats.map(c=>`<button class="filter-btn ${c===active?'active':''}" data-cat="${c}">${c}</button>`).join('');function draw(){let q=search.value.trim().toLowerCase();let list=JW.products.filter(p=>(active==='Tutti'||p.category===active)&&(!q||p.name.toLowerCase().includes(q)||p.badge.toLowerCase().includes(q)));document.querySelector('#result-count').textContent=`${list.length} prodotti`;grid.innerHTML=list.map(p=>`<article class="product-card"><a href="./prodotto.html?id=${p.id}"><div class="product-visual ${p.category==='Office'?'office':p.category.includes('Server')?'server':''}">${p.visual}</div><span class="tag">${p.badge}</span><h3>${p.name}</h3><span class="product-meta">Licenza digitale · Invio via e-mail</span></a><div class="product-bottom"><span class="price">${JW.money(p.price)} <small>IVA esclusa</small></span><button class="mini-btn" data-add="${p.id}" aria-label="Aggiungi ${p.name}">+</button></div></article>`).join('')||'<p>Nessun prodotto trovato.</p>';grid.querySelectorAll('[data-add]').forEach(b=>b.onclick=()=>JW.add(b.dataset.add))}filters.querySelectorAll('button').forEach(b=>b.onclick=()=>{active=b.dataset.cat;filters.querySelectorAll('button').forEach(x=>x.classList.toggle('active',x===b));draw()});search.oninput=draw;draw()});
