document.addEventListener('DOMContentLoaded',()=>{const box=document.querySelector('#featured');if(!box)return;box.innerHTML=JW.products.filter(p=>p.featured).slice(0,4).map(card).join('');box.querySelectorAll('[data-add]').forEach(b=>b.onclick=()=>JW.add(b.dataset.add));function card(p){let cls=p.category==='Office'?'office':p.category.includes('Server')?'server':'';return `<article class="product-card"><a href="./prodotto.html?id=${p.id}"><div class="product-visual ${cls}">${p.visual}</div><span class="tag">${p.badge}</span><h3>${p.name}</h3><span class="product-meta">Licenza digitale · Invio via e-mail</span></a><div class="product-bottom"><span class="price">${JW.money(p.price)} <small>IVA esclusa</small></span><button class="mini-btn" data-add="${p.id}" aria-label="Aggiungi ${p.name}">+</button></div></article>`}});
